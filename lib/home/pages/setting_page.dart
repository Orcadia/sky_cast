import 'package:flutter/material.dart';

class Settingpage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Contenu de la Page Quatre'), // Ajoutez ici le contenu de votre première page
    );
  }
}